<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.my.template.mapper.LoginMapper">
    <select id="dbTest" parameterType="Map" resultType="com.my.common.db.ResultMap">
        SELECT 'Y' AS DB_CONNECTED_YN
          FROM DUAL
    </select>
</mapper>